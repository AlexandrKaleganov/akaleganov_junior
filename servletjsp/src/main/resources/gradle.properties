db.host=jdbc:postgresql://localhost:5432/usersdata
db.login=postgres
db.password=444444
db.driver=org.postgresql.Driver
add.tableUser=create table  if not exists  users(id serial primary key, name varchar(200), login varchar(200), password character(200), create_date Timestamp default now())
