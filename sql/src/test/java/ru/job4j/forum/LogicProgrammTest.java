package ru.job4j.forum;

import org.junit.Test;

import javax.xml.bind.JAXBException;
import javax.xml.transform.TransformerException;
import java.util.ArrayList;

public class LogicProgrammTest {

    @Test
    public void generate() throws JAXBException, TransformerException {
      /*  LogicProgramm logicProgramm = new LogicProgramm();
        Gem gem = new Gem("Щебень", false, "в пещере откапали");
        Gem gem2 = new Gem("Алмаз", true, "в тёмном переулке отобрали");
        ArrayList<Gem> list = new ArrayList<>();
        list.add(gem);
        list.add(gem2);
        Gemmass gemmass = new Gemmass(list);
        System.out.println(logicProgramm.generate(gemmass));
        System.out.println(logicProgramm.convertXMLtoHTML(logicProgramm.generate(gemmass)));*/
    }
}